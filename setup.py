from distutils.core import setup
setup(
  name = 'tweet_classifier',
  packages = ['tweet_classifier'], # this must be the same as the name above
  version = '0.1',
  description = 'A sentiment classifier for tweets',
  author = 'Diego Montufar',
  author_email = 'diogonal0@gmail.com',
  url = 'https://github.com/diogonal/classifier', # use the URL to the github repo
  download_url = 'https://github.com/diogonal/twweet_classifier/tarball/0.1', # I'll explain this in a second
  keywords = ['tweet', 'sentiment', 'classifier'], # arbitrary keywords
  classifiers = [],
)